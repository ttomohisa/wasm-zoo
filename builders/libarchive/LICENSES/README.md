# Third-party notices

The `browser-full` profile links the Emscripten 6.0.6 ports for:

- zlib 1.3.2 — source selected by `tools/ports/zlib.py` in Emscripten 6.0.6.
- bzip2 1.0.6 — source selected by `tools/ports/bzip2.py` in Emscripten 6.0.6.

The corresponding license texts are retained here and copied into binary release bundles.
The exact Emscripten source/ref/commit is recorded in `versions.env` and generated `BUILDINFO.txt`.
