# Start here

Windows: `build.bat browser-full` or `build.bat browser-full-gpl`.

The build must end with a real Chromium smoke-test pass. A Docker link success by itself is not considered a releasable Zoo artifact.
