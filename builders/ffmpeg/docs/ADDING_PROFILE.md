# Adding a full-build profile

A new profile must represent a generally useful distribution tradeoff, not one application feature. Add `profiles/<id>/profile.env` and `ffmpeg.flags`, declare licensing/external libraries, add configure assertions, and extend the real browser smoke test when the new capability is material.
