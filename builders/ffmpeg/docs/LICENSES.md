# License model

`browser-full` avoids `--enable-gpl` and optional GPL libraries. `browser-full-gpl` enables GPL components and links x264. The generated binary license is therefore profile-specific and must be stated in metadata and release bundles.
